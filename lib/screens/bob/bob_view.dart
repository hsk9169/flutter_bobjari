import 'package:flutter/material.dart';

class BobView extends StatelessWidget {
  const BobView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('bob');
  }
}
